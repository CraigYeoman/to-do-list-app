// const task = (title, project, priority, dueDate, completion, description, notes) {
//     this.title = title;
//     this.project = project;
//     this.priority = priority;
//     this.dueDate = dueDate;
//     this.completion = completion;
//     this.description = description;
//     this.notes = notes;
// }



const task = (title, project, priority, dueDate, completion, description, notes) => {
    return {title, project, priority, dueDate, completion, description, notes};
}

export default task;
